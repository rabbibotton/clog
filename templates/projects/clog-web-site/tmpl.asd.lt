(asdf:defsystem #:<%= (@ sys-name) %>
  :description "New CLOG Websight System"
  :author "some@one.com"
  :license  "BSD"
  :version "0.0.0"
  :serial t
  :depends-on (#:clog)
  :components ((:file "<%= (@ sys-name) %>")))

